
<%= form_for @user, url: { action: "create" } do |f| %>
	<%= f.label :username %>
	<%= f.text_field :username %>
	<%= f.label :email %>
	<%= f.text_field :email, placeholder:"example@me.com"%>
	<%= f.label :password %>
	<%= f.password_field :password %>
	<%= f.submit "submit" %>
<% end %>